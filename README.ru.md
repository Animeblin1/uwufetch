# 🌸 UwU Fetch для PowerShell

Kawaii скрипт для PowerShell с ASCII-артом и милыми фразочками~

**Русский** | [English](README.md)

## ✨ Возможности

- 🎨 Красивый UwU ASCII-арт
- 💖 Случайные милые фразы при каждом запуске
- 📊 Системная информация (ОС, CPU, GPU, RAM, Диск, IP)
- 🚀 Автозапуск при старте PowerShell
- 🎯 Установка одной командой
- ✅ Автоматическая установка PowerShell 7 при необходимости

## 📦 Установка

### Быстрая установка

Открой **PowerShell** (любую версию) и выполни:

```powershell
irm https://raw.githubusercontent.com/Animeblin1/uwufetch/main/install.ps1 | iex
```

> ⚡ Если у тебя нет PowerShell 7, установщик автоматически установит его через `winget`

### Ручная установка

1. Скачай `install.ps1`
2. Запусти в PowerShell:

```powershell
.\install.ps1
```

## 🎮 Использование

UwU Fetch автоматически запускается при старте PowerShell.

Для ручного запуска:

```powershell
uwufetch
```

## 🗑️ Удаление

### Быстрое удаление

```powershell
irm https://raw.githubusercontent.com/Animeblin1/uwufetch/main/uninstall.ps1 | iex
```

### Ручное удаление

1. Скачай `uninstall.ps1`
2. Запусти в PowerShell:

```powershell
.\uninstall.ps1
```

### Удаление вручную (без скрипта)

Открой профиль PowerShell:

```powershell
notepad $PROFILE
```

Удали эти строки и сохрани:

```powershell
. "C:\fetch\uwu_fetch.ps1"
function uwufetch { & "C:\fetch\uwu_fetch.ps1" }
```

Затем удали папку:

```powershell
Remove-Item C:\fetch -Recurse -Force
```

## 📸 Превью

```
  ██╗   ██╗  ██╗    ██╗  ██╗   ██╗
  ██║   ██║  ██║    ██║  ██║   ██║
  ██║   ██║  ██║ █╗ ██║  ██║   ██║
  ██║   ██║  ██║███╗██║  ██║   ██║
  ╚██████╔╝  ╚███╔███╔╝  ╚██████╔╝
   ╚═════╝    ╚══╝╚══╝    ╚═════╝


                                user-chan~ ~ 🌸 DESKTOP-PC
                                         -------------------------
                                      OS: Windows 11 Pro
                    Uptime: 2 days, 5 hours, 13 minutes
                                    Local IP: 192.168.1.42
                       Motherboard: ASUS ROG STRIX B550-F
                            CPU: AMD Ryzen 7 5800X
                            GPU: NVIDIA GeForce RTX 3070
                 Memory: 12.3 GiB / 31.9 GiB (38.6%)
              Disk (C:): 234.1 GiB / 476.9 GiB (49.1%)

✨ nya~ nya~ everything is kawaii uwu
```

## 🛠️ Требования

- **Windows 10/11**
- **PowerShell 7+** (автоматически устанавливается при необходимости)
- **winget** (предустановлен в Windows 11, доступен в Windows 10 через [App Installer](https://apps.microsoft.com/detail/9nblggh4nns1))

### Опциональные требования

- **Интернет-соединение** (для установки одной командой)
- **Права администратора** (только если нужно изменить политику выполнения)

## 📁 Структура файлов

```
C:\fetch\
└── uwu_fetch.ps1          # Основной скрипт

Documents\PowerShell\
└── Microsoft.PowerShell_profile.ps1   # Профиль с автозагрузкой (PS 7)
```

или

```
Documents\WindowsPowerShell\
└── Microsoft.PowerShell_profile.ps1   # Профиль (PS 5.1)
```

## 🔧 Решение проблем

### "uwufetch is not recognized" после установки

Функция ещё не загружена. Перезагрузи профиль:

```powershell
. $PROFILE
```

Или перезапусти PowerShell.

### Проверка установки

Проверь существование скрипта:

```powershell
Test-Path C:\fetch\uwu_fetch.ps1
```

Проверь наличие функции в профиле:

```powershell
Get-Content $PROFILE | Select-String "uwufetch"
```

Загрузи функцию вручную:

```powershell
function uwufetch { & "C:\fetch\uwu_fetch.ps1" }
```

### PowerShell 7 не устанавливается автоматически

Установи вручную:

```powershell
winget install Microsoft.PowerShell
```

Или скачай с: https://aka.ms/powershell

### Ошибка "Execution policy"

Запусти от имени администратора:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Скрипт не запускается при старте

Проверь существование профиля:

```powershell
Test-Path $PROFILE
Get-Content $PROFILE
```

Перезагрузи вручную:

```powershell
. $PROFILE
```

### Ошибка "winget not found"

Установи App Installer из Microsoft Store:
https://apps.microsoft.com/detail/9nblggh4nns1

## 🤝 Вклад в проект

Приветствуются любые улучшения:

- 🐛 Сообщения об ошибках
- 💡 Предложения новых функций
- 🎨 Улучшение ASCII-арта
- 📝 Исправление документации
- ✨ Добавление новых kawaii фраз

## 📝 Лицензия

MIT License — свободно используй и модифицируй!

## 💖 Авторы

Сделано с любовью и >w<

Вдохновлено neofetch и другими fetch-утилитами~

---

⭐ **Поставь звезду, если понравилось!** ⭐

[![Star History Chart](https://api.star-history.com/svg?repos=Animeblin1/uwufetch&type=date&legend=top-left)](https://www.star-history.com/#Animeblin1/uwufetch&type=date&legend=top-left)
